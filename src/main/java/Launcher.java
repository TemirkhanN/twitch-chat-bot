import javafx.application.Application;

public class Launcher {   // <=== note - does not extend Application
    public static void main(String[] args) {
        Application.launch(Main.class);
    }
}